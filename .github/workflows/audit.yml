name: Audit

on:
    push:
        branches: [ master ]
    pull_request:
    schedule:
        - cron: '0 9 * * *' # Daily @ 09:00:00 UTC
    workflow_dispatch:

concurrency:
    # Cancel in-progress runs on branch updates
    group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
    cancel-in-progress: true
env:
    CARGO_TERM_COLOR: always
    RBMT_LOG_LEVEL: verbose
permissions: {}

jobs:
    cargo-audit:
        name: Cargo Audit - ${{ matrix.name }}
        runs-on: ubuntu-latest
        permissions:
            contents: read
            checks: write
            issues: write
        strategy:
            fail-fast: false
            matrix:
                include:
                    - name: Fresh Lockfile
                      lockfile: Cargo.lock
                    - name: Maximum Lockfile
                      lockfile: Cargo-maximum.lock
                    - name: Recent Lockfile
                      lockfile: Cargo-recent.lock
                    - name: Minimal Lockfile
                      lockfile: Cargo-minimal.lock

        steps:
            - name: Checkout Repository
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
              with:
                  persist-credentials: false

            - name: Setup Rust Cache
              uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1

            - name: Setup cargo-rbmt
              uses: $/.github/actions/setup-rbmt

            - name: Generate Fresh Lockfile
              if: ${{ matrix.lockfile == 'Cargo.lock' }}
              shell: bash
              run: cargo generate-lockfile

            - name: Select Lockfile
              if: ${{ matrix.lockfile != 'Cargo.lock' }}
              shell: bash
              run: cp ${{ matrix.lockfile }} Cargo.lock

            - uses: rustsec/audit-check@69366f33c96575abad1ee0dba8212993eecbe998 # v2.0.0
              with:
                  token: ${{ secrets.GITHUB_TOKEN }}

    shellcheck:
        name: ShellCheck
        runs-on: ubuntu-latest
        steps:
            - name: Checkout Repository
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
              with:
                  persist-credentials: false

            - name: Run ShellCheck
              shell: bash
              run: find . -name '*.sh' -print -exec shellcheck {} +

    zizmor:
        name: Zizmor
        runs-on: ubuntu-latest

        steps:
            - name: Checkout Repository
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
              with:
                  persist-credentials: false

            - name: Setup Rust Cache
              uses: Swatinem/rust-cache@c19371144df3bb44fab255c43d04cbc2ab54d1c4 # v2.9.1

            - name: Setup cargo-rbmt
              uses: $/.github/actions/setup-rbmt

            - name: Run Zizmor
              shell: bash
              run: zizmor .
